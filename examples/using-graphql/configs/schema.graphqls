type User {
    id: Int
    name: String
    role: String
}

type Query {
    hello: String
    user: User
    users: [User]
    getUser(id: Int): User
}

type Mutation {
    createUser(name: String, role: String): User
}
