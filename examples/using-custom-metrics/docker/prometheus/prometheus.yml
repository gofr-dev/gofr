global:
  scrape_interval: 5s

scrape_configs:
  # Metrics your app pushed via OTLP, re-exposed by the collector.
  - job_name: otel-collector
    static_configs:
      - targets: ["otel-collector:8889"]
